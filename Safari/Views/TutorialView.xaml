<Page x:Class="Safari.Views.TutorialView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="745" d:DesignWidth="1215"
      Title="Tutorial"
      Background="Bisque">
    <Page.Resources>
        <!-- Custom ScrollBar Style -->
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="SandyBrown"/>
            <Setter Property="BorderBrush" Value="SaddleBrown"/>
            <Setter Property="Width" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" 
                            CornerRadius="6"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"/>
                            <Track x:Name="PART_Track" 
                                   IsDirectionReversed="True"
                                   Orientation="Vertical">
                                <Track.Thumb>
                                    <Thumb Background="SaddleBrown" 
                                   BorderBrush="Sienna"
                                   BorderThickness="1">
                                        <Thumb.Style>
                                            <Style TargetType="Thumb">
                                                <Setter Property="MinHeight" Value="30"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="Peru"/>
                                                    </Trigger>
                                                    <Trigger Property="IsDragging" Value="True">
                                                        <Setter Property="Background" Value="Sienna"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Thumb.Style>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ScrollViewer">
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="FlowDirection" Value="LeftToRight"/>
        </Style>
    </Page.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Top" BorderBrush="SaddleBrown" BorderThickness="4" 
                Height="60" CornerRadius="20" Background="SandyBrown">
            <TextBlock FontSize="30" Background="Transparent"
                       HorizontalAlignment="Center" VerticalAlignment="Center">
                ----------------------------------------------------------------------------------------Tutorial----------------------------------------------------------------------------------------
            </TextBlock>
        </Border>
        <Border DockPanel.Dock="Bottom" BorderBrush="SaddleBrown" BorderThickness="4" 
                CornerRadius="20" Background="SandyBrown">
            <Button FontSize="30"
                    VerticalContentAlignment="Stretch" HorizontalContentAlignment="Center"
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    Content="Back" Command="{Binding BackCommand}" Width="300">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="Black"
                                BorderThickness="2" 
                                CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Border>
        <Border BorderBrush="SaddleBrown" BorderThickness="2" CornerRadius="10" 
                Background="SandyBrown" Margin="0 5">
            <ScrollViewer>
                <TextBlock Text="{Binding TutorialText, Mode=OneWay}" 
                           FontSize="18" 
                           Background="Transparent"
                           TextWrapping="Wrap"
                           Padding="15"
                           Focusable="True"/>
            </ScrollViewer>
        </Border>
        
    </DockPanel>
</Page>